function recipesReducer(
    state = {
        recipes: [],
    }, action
    ) {
    switch (action.type) {
        case 'ADD_RECIPE':
            return
        default:
            return state
}

export default recipesReducer
