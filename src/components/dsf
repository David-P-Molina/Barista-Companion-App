fetch('http://localhost:3001/coins')
        .then(res => {
            if (res.ok) {
                return res.json()
                .then(coins => dispatch({ type: ADD_COINS, coins}))
            } else {
                return res.json()
                    .catch((errors) => {
                    console.log(errors)
                })
            }
        })