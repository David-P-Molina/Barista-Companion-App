import React from 'react'

const Error = ({props}) => {

    return (
        <div className='error-message'>
            {console.log(props)}
            {props}
        </div>
    )
}

export default Error